import { GlobalExeptionFilter } from './global-exeption.filter';

describe('GlobalExeptionFilter', () => {
  it('should be defined', () => {
    expect(new GlobalExeptionFilter()).toBeDefined();
  });
});
